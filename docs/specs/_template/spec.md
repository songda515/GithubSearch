# <Feature> Spec

> 이 화면 정책의 **SSoT** 다. 동작을 바꾸려면 코드보다 **이 문서를 먼저** 고친다.
> 새 스펙은 이 템플릿을 복사해 작성한다. 미정 항목은 빈칸 대신 `TBD`로 명시한다.

| 항목 | 값 |
|------|----|
| Feature | `<feature-slug>` |
| 상태 | Draft \| Approved \| Implemented \| Deprecated |
| 관련 이슈/PR | #__ |
| 최종 수정 | YYYY-MM-DD |

---

## 1. 개요 (Overview)
- **한 줄 설명**: 이 화면이 사용자에게 무엇을 제공하는가.
- **진입 경로**: 어디서 어떻게 이 화면에 도달하는가.
- **TCA 모듈**: `View` = `<Name>View`, `Reducer` = `<Name>Feature`.

## 2. 화면별 요구사항 (Requirements)
사용자 관점의 기능 요구사항. 각 항목은 검증 가능하게(테스트로 옮길 수 있게) 쓴다.

- R1. (사용자가 …하면 …한다)
- R2. …

## 3. UI 상태 (UI States)
화면이 가질 수 있는 상태와 각 상태의 표시 규칙. TCA `State` 설계의 근거가 된다.

| 상태 | 조건 | 화면 표시 |
|------|------|-----------|
| Idle / 초기 | 진입 직후 | … |
| Loading | 요청 진행 중 | … |
| Loaded | 성공 | … |
| Empty | 성공이나 결과 0건 | … |
| Error | 실패 | … |

## 4. 엣지 케이스 (Edge cases)
정상 경로 밖의 입력·상황과 기대 동작.

| # | 상황 | 기대 동작 |
|---|------|-----------|
| E1 | 빈 입력 / 공백만 입력 | … |
| E2 | 네트워크 오프라인 | … |
| E3 | 요청 중 입력 변경(취소/디바운스) | … |
| E4 | 페이지네이션 마지막 페이지 | … |
| E5 | 권한/인증 만료 | … |

## 5. API 명세 (API spec)
이 화면이 호출하는 외부 API. 구현 시 `Client` 의 closure 시그니처로 옮긴다.

### `<동작명>` — `GET /path`
- **요청 파라미터**

  | 이름 | 타입 | 필수 | 설명 |
  |------|------|------|------|
  | `q` | String | Y | … |

- **응답 (200)**: 핵심 필드와 매핑할 모델
- **에러 코드**: `401` …, `403` …, `422` …, `5xx` …
- **레이트리밋/페이지네이션**: 헤더·파라미터 규칙

## 6. 정책 (Policy)
비즈니스 규칙·검증·제약. "왜 이렇게 동작하는가"의 근거.

- P1. (입력 검증: …)
- P2. (디바운스/스로틀: …)
- P3. (정렬/필터 기본값: …)
- P4. (캐싱/리트라이: …)

## 7. TCA 매핑 (State / Action / Client)
스펙을 하네스로 잇는 다리. 구현·테스트의 출발점.

- **State**: 위 UI 상태/요구사항을 표현하는 `@ObservableState` 필드.
- **Action**: 사용자 입력·시스템 이벤트·effect 결과.
- **Client**: 5번 API를 감싸는 `@DependencyClient` (live/test/preview 3종).

## 8. 수용 기준 (Acceptance criteria)
이 스펙이 "구현됨"으로 인정되는 조건. `TestStore` 시나리오로 옮긴다.

- [ ] R1 → 테스트: …
- [ ] E2 → 테스트: …
- [ ] P2 → 테스트: …

## 9. 변경 이력 (Changelog)
| 날짜 | 이슈/PR | 변경 내용 |
|------|---------|-----------|
| YYYY-MM-DD | #__ | 최초 작성 |
